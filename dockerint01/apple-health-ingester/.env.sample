# Copy to .env: cp .env.sample .env

# Bearer token required to authenticate ingest requests from Health Auto Export.
# Generate with: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
# The Health Auto Export automation must send this as: Authorization: Bearer <token>
AUTH_TOKEN=change_me
