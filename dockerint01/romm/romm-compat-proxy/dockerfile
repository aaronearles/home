FROM python:3.12-slim

WORKDIR /app

RUN pip install --no-cache-dir fastapi uvicorn httpx

COPY proxy.py .

ENV ROMM_UPSTREAM=http://romm:8080
ENV LISTEN_PORT=8888
ENV LOG_LEVEL=INFO

EXPOSE 8888

CMD ["python", "proxy.py"]
