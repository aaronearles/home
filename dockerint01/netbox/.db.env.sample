# PostgreSQL Configuration
# Copy to .db.env and fill in actual values
# DB_NAME, DB_USER, DB_PASSWORD must match values in .env

POSTGRES_DB=netbox
POSTGRES_USER=netbox
POSTGRES_PASSWORD=your_db_password
